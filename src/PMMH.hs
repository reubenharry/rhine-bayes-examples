module PMMH where


-- you have an unknown mean where the object originated

-- setup :: Double -> SF () Position 

-- obsMod :: SF Position Observation

-- prior :: Distribution Double

-- posterior :: Distribution Double 
-- posterior = do
--     p <- prior
--     factor byStuff
--     return p

-- belief over a changing thing    versus   a changing belief over a thing


-- pmmh :: MonadSample m => Traced (Weighted m) a -> SF a b -> SF a (Population (Weighted b))
-- pmmh = undefined